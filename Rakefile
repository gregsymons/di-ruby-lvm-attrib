# -*- ruby -*-

require 'rubygems'
require 'hoe'
$:.unshift(File.dirname(__FILE__) + "/lib")
require 'lvm/attributes'

Hoe.new('ruby-lvm-attrib', LVM::Attributes::VERSION) do |p|
  p.developer('Matthew Kent', 'matt@bravenet.com')
  p.extra_deps << ['ruby-lvm', '>= 0.0.2']
end

# vim: syntax=Ruby
